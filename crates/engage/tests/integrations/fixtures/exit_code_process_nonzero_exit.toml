[processes.a]
command = [
    "bash", "-euo", "pipefail", "-c",

    """
    echo goodbye world
    exit 7
    """
]
ready-when = "exited"

[processes.b]
command = ["echo", "this should never be run"]
ready-when = "exited"
after = ["a"]
