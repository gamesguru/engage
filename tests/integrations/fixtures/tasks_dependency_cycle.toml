interpreter = ["bash", "-euo", "pipefail", "-c"]

[tasks.a]
script = "echo hello world"
after = ["b"]

[tasks.b]
script = "echo hello world"
after = ["a"]

[tasks.c]
script = "echo hello world"
after = ["e"]

[tasks.d]
script = "echo hello world"
after = ["c"]

[tasks.e]
script = "echo hello world"
after = ["d"]
