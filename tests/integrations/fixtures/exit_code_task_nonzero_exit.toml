version = "0.0.0-dev"

[tasks.task]
command = [
    "bash", "-euo", "pipefail", "-c",

    """
    echo goodbye world
    exit 7
    """
]

[tasks.nope]
command = ["echo", "this should never be run"]
after = ["task"]
